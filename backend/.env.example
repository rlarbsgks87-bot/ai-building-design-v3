# Django
DJANGO_SECRET_KEY=your-secret-key-here
DJANGO_SETTINGS_MODULE=config.settings.development

# Database (개발용 SQLite 사용 시 불필요, Render에서 자동 설정)
DATABASE_URL=postgres://user:password@localhost:5432/ai_building

# Redis (개발용 로컬메모리 캐시 사용 시 불필요)
REDIS_URL=redis://localhost:6379/0

# VWorld API (vworld.kr에서 발급)
# Lambda 프록시 사용 시 선택사항
VWORLD_API_KEY=your-vworld-api-key

# 공공데이터포털 API (data.go.kr에서 발급)
# 동일한 키로 다음 API 사용 가능:
# - 건축물대장정보 (BldRgstHubService) - 일일 10,000건
# - 토지이용규제정보 (arLandUseInfoService) - 일일 1,000건
# - 토지이용규제법령 (LuLawInfoService) - 일일 1,000건
DATAGO_API_KEY=your-datago-api-key

# AWS Lambda 프록시 (VWorld CORS 우회)
# 기본값이 설정되어 있어 변경 불필요
LAMBDA_PROXY_URL=https://3a9op0tcy6.execute-api.ap-northeast-2.amazonaws.com/prod/
